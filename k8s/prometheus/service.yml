apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: event-monitor
  labels:
    app: prometheus
spec:
  type: ClusterIP
  selector:
    app: prometheus
  ports:
  - port: 9090
    targetPort: 9090
    protocol: TCP
    name: web
