apiVersion: v1
kind: Service
metadata:
  name: event-monitor-service
  namespace: event-monitor
  labels:
    app: event-monitor
spec:
  type: ClusterIP
  selector:
    app: event-monitor
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
