apiVersion: v1
kind: Service
metadata:
  name: event-monitor-service
  namespace: event-monitor
  labels:
    app: event-monitor
spec:
  type: NodePort
  selector:
    app: event-monitor
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30081
    protocol: TCP
    name: http
